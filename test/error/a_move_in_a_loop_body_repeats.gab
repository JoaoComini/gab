struct Box { n: i32 }

func f(): i32 {
    let a: *Box = box Box { n: 0 };
    for let i = 0; i < 2; i = i + 1 {
        let b = a;
    }
    return 0;
}

// error
// :6:17: lifetime error: this was moved out of and no longer holds a value
