struct Node { n: i32 }

struct Pair { a: &Node, b: &Node }

func f(p: &Node): Pair {
    let l = Node { n: 1 };
    let x = Pair { a: p, b: l };
    return x;
}

// error
// :8:5: lifetime error: this borrow outlives what it names, so it cannot be returned
