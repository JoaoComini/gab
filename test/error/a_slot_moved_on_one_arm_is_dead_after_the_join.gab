struct Box { n: i32 }

func f(): i32 {
    let a: Unique<Box> = Unique<Box>::new(Box { n: 0 });
    if 1 < 2 { let b = a; } else { }
    return (*a).n;
}

// error
// :6:14: lifetime error: this was moved out of and no longer holds a value
