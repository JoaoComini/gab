struct Box { n: i32 }

func f(): i32 {
    let a: Unique<Box> = Unique<Box>::new(Box { n: 0 });
    for let i = 0; i < 2; i = i + 1 {
        if i == 1 { let b = a; break; }
    }
    return (*a).n;
}

// error
// :8:14: lifetime error: this was moved out of and no longer holds a value
