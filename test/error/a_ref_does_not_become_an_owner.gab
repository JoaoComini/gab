struct Node { n: i32 }

func f(): i32 {
    let owner: Unique<Node> = Unique<Node>::new(Node { n: 0 });
    let b: &Node = *owner;
    let o: Unique<Node> = b;
    return 0;
}

// error
// :6:27: type error: cannot initialize a variable of type Unique with a value of type &Node
