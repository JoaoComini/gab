struct Unique { n: i32 }

impl Unique {
    func take(self: Unique): i32 { return self.n; }
}

func f(): i32 {
    let h: Unique = Unique { n: 0 };
    h.take();
    return h.n;
}

// error
// :10:13: lifetime error: this was moved out of and no longer holds a value
