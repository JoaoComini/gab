struct Node { n: i32 }

func f(): &Node {
    let local: Unique<Node> = Unique<Node>::new(Node { n: 0 });
    let s: &Unique<Node> = local;
    return **s;
}

// error
// :6:5: lifetime error: this names memory that has been freed
