struct Box { n: i32 }

func replace(slot: &&Box): i32 { return 0; }

func f(): i32 {
    let o: Unique<Box> = Unique<Box>::new(Box { n: 0 });
    return replace(o);
}

// error
// :7:20: type error: argument 1 is Unique, but &&Box was declared
