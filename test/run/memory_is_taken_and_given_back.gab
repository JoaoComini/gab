module test;
import testing;
import libc;

func run(): i32 {
    let bytes: raw<u8> = libc::malloc(@size_of<i32>() * 4);
    let p: raw<i32> = raw<i32>(bytes);

    p[0] = 1;
    p[3] = 8;

    assert(p[0] + p[3] == 9);

    libc::free(bytes);

    return 0;
}
