func at(xs: &array<i32, 3>, i: i32): i32 { return xs[i]; }

func run(): i32 {
    let xs: array<i32, 3> = [1, 2, 3];

    assert(at(xs, 0) == 1);
    assert(at(xs, 2) == 3);

    let past: i32 = 3;
    at(xs, past);

    return 0;
}

// traps
