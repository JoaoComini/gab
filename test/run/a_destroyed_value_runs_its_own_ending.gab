module test;
import testing;

struct Counter { n: &i32 }

impl Counter as Destroy {
    func destroy(self: &Self) {
        *self.n = *self.n + 1;
    }
}

func run(): i32 {
    let seen: i32 = 0;

    { let c: Counter = Counter { n: seen }; }

    assert(seen == 1);

    return 0;
}
